# Validator dependencies — core loop + image pipeline, no DL
# Usage:  pip install -e ".[validator,validator-image]"
# Docker: pip install -r requirements.validator.txt && pip install -e "." --no-deps
#
# To strip image processing (opencv, scikit-image) as well:
#   pip install -e ".[validator]"

# Core
bittensor>=9.9.0,<10.0.0
bittensor-cli>=9.18.0,<10.0.0
httpx>=0.28.0
pillow>=10.4.0
loguru>=0.7.2
h11>=0.16.0
cryptography>=46.0.3
cyscale>=0.3.3
pyyaml>=6.0
numpy>=1.26.0,<2.0

# Validator core
wandb>=0.19.0
joblib>=1.4.2
tensorboardx>=2.6.2.2

# Image pipeline: HuggingFace cache download + augmentation
datasets>=3.1.0
opencv-python>=4.10.0.84
scikit-image>=0.24.0
scikit-learn>=1.5.2
